import { Box } from '@mui/material'

const Toolbar = () => {
  return (
    <Box
      sx={{
        width: '120px',
        padding: '50px 20px',
        background: '#FFF',
      }}
    ></Box>
  )
}

export default Toolbar
